Telco Demo backend (MySQL-ready)

- Update src/main/resources/application.yml with your MySQL credentials.
- Create DB using schema-mysql.sql (provided at root of zip).
- Build and run: mvn -f backend/pom.xml spring-boot:run
